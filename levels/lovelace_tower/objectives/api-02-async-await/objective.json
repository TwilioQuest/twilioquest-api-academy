{
  "title": "API Academy: Async Await",
  "description": "Write an async function that gets the last file in a directory using await.",
  "show_ide": true,
  "validation_fields": [
    {
      "name": "prompt",
      "type": "prompt",
      "label": "Write a function called \"findLastFileInDir\" that gets and returns the last file in a directory."
    }
  ],
  "rewards": {
    "xp": 100
  }
}
